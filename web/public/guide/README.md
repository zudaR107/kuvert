# Guide screenshots

Drop screenshots referenced by `src/features/help/HelpPage.tsx` here, named
`kuvert-<slug>.png` (see the `<img>` tags in that file for the exact
filenames expected). No code change is needed once a matching file exists —
Vite serves this whole directory as-is from the site root.
